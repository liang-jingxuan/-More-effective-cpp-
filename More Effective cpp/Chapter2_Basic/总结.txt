这里的基础议题分别是:
1.引用和指针的区别
    1.1、引用不能指向空值
    1.2、引用的“=”操作符是赋值操作
    结论：如果一个东西可能指向空值，则用指针；如果会改变某个变量的指向，则用指针
2.类型转换
    2.1 static_cast 静态转换,不具备继承关系的类型之间转换,比如int和double
    2.2 const_cast 去掉变量的const属性
    2.3 dynamic_cast 使指针指向对象从父类转换为某个子类
3.不要对有继承关系的类定义一个数组
4.避免无用的缺省构造函数（指没有参数的构造函数）